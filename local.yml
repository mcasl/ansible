- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: Actualizar los repositorios de apt
      apt:
        update_cache: yes
      changed_when: false

  tasks:
#    - include: tasks/factos.yml
    - include: tasks/cron_job_ansible_pull.yml
    - include: tasks/paquetes_apt.yml
    - include: tasks/anaconda.yml
    - include: tasks/rustup_root.yml
    - include: tasks/rustup_mcasl.yml
    - include: tasks/paquetes_cargo.yml
    - include: tasks/tailscale.yml
#    - include: tasks/email_final.yml
    
    


